$kind: http-request
description: |-
  Creates a new attribute definition for the category. Required fields: `attributeName`, `valueType`, `isRequired`.

  **Expected:** 201 Created
url: "{{category_service}}/api/categories/1/attributes"
method: POST
body:
  type: json
  content: |-
    {
      "attributeName": "Chipset",
      "valueType": "string",
      "isRequired": true
    }
order: 5000
