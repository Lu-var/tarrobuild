$kind: http-request
description: |-
  Adds a new attribute to a product. Required fields: `attributeName`, `attributeValue`.

  **Expected:** 201 Created
url: "{{product_service}}/api/products/1/attributes"
method: POST
order: 15000
body:
  type: json
  content: |-
    {
      "attributeName": "Socket",
      "attributeValue": "LGA1700"
    }
tests:
  - name: "Returns 201"
    statusCode: 201