$kind: http-request
url: "{{tarrobuild}}{{api_prefix}}/auth/login"
method: POST
body:
  type: json
  content: |-
    {
      "email": "admin@tarrobuild.cl",
      "password": "admin123"
    }
order: 150
