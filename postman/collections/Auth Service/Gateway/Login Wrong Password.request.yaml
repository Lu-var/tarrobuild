$kind: http-request
url: "{{api_gateway}}{{api_prefix}}/auth/login"
method: POST
body:
  type: json
  content: |-
    {
      "email": "admin@tarrobuild.cl",
      "password": "wrongpassword"
    }
order: 190
